python -m App_PADESCE.core.runtime_bootstrap; python manage.py migrate --noinput || echo "[start] migrate a echoue (non bloquant)"; python manage.py collectstatic --noinput || echo "[start] collectstatic a echoue (non bloquant)"; exec gunicorn App_PADESCE.asgi:application -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:${PORT:-8888} --workers ${WEB_CONCURRENCY:-2} --timeout ${WEB_TIMEOUT:-180} --keep-alive ${WEB_KEEP_ALIVE:-5} --access-logfile - --error-logfile -
